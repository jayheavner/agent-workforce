{
  "description": "Write lanes the workforce guards enforce. Shipped defaults; a project overrides a key by declaring it in its own .workforce/project.json. A key that cannot be read falls back to the guard's built-in default, never to 'no rule'.",
  "acceptance_suite_paths": [
    "tests/acceptance"
  ],
  "doc_paths": [
    "docs",
    "plans",
    "doc-inventory"
  ],
  "role_lanes": {
    "scribe": ["docs", "plans", "doc-inventory"],
    "architect": ["plans", "docs", "skills", "agents"],
    "test-author": ["tests"]
  }
}
